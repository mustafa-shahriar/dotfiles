### Basic terminal fixes
set -g terminal-overrides ',*:RGB'

### Prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

### Window indexing
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

### Vi copy mode
set -g @plugin 'tmux-plugins/tmux-yank'
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

### Fast window switching (Alt + number)
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

### Alt+< and > for previous/next window
bind -n M-. next-window
bind -n M-, previous-window

### Minimal statusbar (optional, lightweight)
set -g status-style "bg=#0f111a,fg=white"
set -g window-status-current-style "bg=#0f111a,fg=green"
set -g window-status-style "bg=#0f111a,fg=white"
set -g message-style "bg=#0f111a,fg=white"

run '~/.tmux/plugins/tpm/tpm'
